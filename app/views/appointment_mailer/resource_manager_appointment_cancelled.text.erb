An appointment was cancelled <%= '(online)' if @appointment.cancelled_by_customer_online? %>

Reference number:
<%= @appointment.id %>

Guider:
<%= @appointment.guider.name %>

<%= edit_appointment_url(@appointment) %>
