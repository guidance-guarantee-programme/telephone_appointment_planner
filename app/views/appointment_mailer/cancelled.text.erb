We've cancelled your appointment

Dear <%= @appointment.first_name %>,

We have cancelled the following phone appointment as requested:

Date:
<%= @appointment.start_at.to_date.to_s(:govuk_date) %>

Time:
<%= "#{@appointment.start_at.to_time.to_s(:govuk_time)} (#{@appointment.timezone})" %>

Reference number:
<%= @appointment.id %>

If you didn't ask us to cancel your appointment or to book a new one, call 0800 138 3944. Please don't reply to this email.
